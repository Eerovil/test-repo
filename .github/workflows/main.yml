name: 'Master -> Develop merge'

on:
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: protecomp/ga-merge-branch
      with:
        source_branch: master
        target_branch: develop
